# BSC KU

موقع نادي العلوم البيولوجية (جامعة الكويت) + تطبيق الجوال، من نفس الكود ونفس الـ backend.

## البنية

- **الموقع**: صفحات HTML ثابتة في جذر المشروع، مستضافة على GitHub Pages (biosci-ku.com).
- **التطبيق**: Capacitor يلفّ نفس صفحات الموقع — أندرويد و iOS.
- **البيانات**: Firebase (مشروع `bsc-ku`) — Firestore + Auth مجهول. نفس المشروع للموقع والتطبيق، فأي تحديث محتوى يظهر بالاثنين فوراً.

## قاعدة مهمة: `www/` مجلد مولّد

لا تعدّل داخل `www/` أبداً — ينمسح ويتولّد من جديد. عدّل على:

- **صفحات الموقع** في الجذر (تنعكس على الموقع والتطبيق).
- **`app-shell/`** — واجهة التطبيق فقط (الشريط السفلي، الهيدر، تعديلات التخطيط للجوال).

بعد أي تعديل:

```bash
npm run build:app
```

يبني `www/` ثم ينقلها للمنصتين. (`npm run sync` يبني `www/` بس.)

## إضافة صفحة جديدة للتطبيق

ضِف اسمها في مصفوفة `PAGES` في `.claude/sync-app.js`، وضِف رابطها في قائمة "المزيد" في `app-shell/app-nav.js`.
السكربت يكتشف ملفات CSS/JS/الصور المرتبطة تلقائياً، إلا اللي تنستدعى من داخل جافاسكربت — هذي تنضاف يدوياً في `EXTRA_ASSETS`.

`admin.html` مستثناة عمداً: لوحة الإدارة تبقى على الويب وما تنشحن مع التطبيق.

## التجربة محلياً

```bash
npm run dev
```

الموقع على `localhost:5173`، ونسخة التطبيق على `localhost:5174` عبر `node .claude/dev-server-app.js`.
لازم `localhost` — Firebase ما يشتغل من `file://`.
